# פרויקט: ציר הזמן של עם ישראל (SimpleIsrael)

אתר עברי RTL על ההיסטוריה המקראית — מהאבות ועד חורבן בית שני. חי ב-**simpleisrael.co.il**,
ריפו `talyehuda2/SimpleIsrael`, נפרס אוטומטית ב-Vercel בכל דחיפה ל-`main`.

**כל השנים באתר הן לבריאה לפי סדר עולם, לא לספירה.** זו לא קוסמטיקה — טקסט, נתונים
וכל תשובה שהאתר נותן חייבים להיצמד לזה. אל תמיר ואל תערבב.

## המבנה: ארבע נקודות כניסה, לא אחת

זו העובדה החשובה ביותר בפרויקט, והמקור לרוב הבאגים.

| מסך | כתובת | HTML | קוד | גיליון סגנונות |
|---|---|---|---|---|
| ציר הזמן | `/` | `index.html` | `src/main.jsx` → `src/App.jsx` (React) | `src/styles.css` |
| מסע הדורות | `/atlas` | `atlas.html` | `src/atlas/main.js` (vanilla + איי React) | `src/atlas/atlas.css` |
| מפת הארץ | `/places` | `places.html` | `src/places/main.js` (vanilla) | `src/places/places.css` |
| ניהול | `/admin` | `admin.html` | `src/admin/main.jsx` (React) | `src/admin/admin.css` |

`vercel.json` מגדיר rewrites ל-`/atlas`, `/places`, `/admin` ו-`/privacy`.

`/admin` אינו חלק מהאתר הציבורי: אין אליו קישור נכנס, הוא חסום ב-`robots.txt`,
ואינו ב-sitemap. ההגנה עצמה היא טוקן הניהול, לא ההסתרה.

### ⚠️ המלכודת מספר אחת: רכיב משותף שמסתמך על גיליון של מסך אחד

שלושת הגיליונות נפרדים, ולכן רכיב שסגנונו יושב רק ב-`styles.css` עלול להיראות תקין
בציר הזמן ו**להופיע בלי שום עיצוב** במסך אחר. אבל לא כל רכיב משותף מגיע לכל מסך —
זה מאומת מול הייבוא בפועל (אוגוסט 2026):

| רכיב | מרונדר מ־ | מסכים |
|---|---|---|
| `NotesBox` | `App.jsx` + `lib/notes.jsx` (נטען מ-`atlas/main.js` ומ-`places/main.js`) | **שלושתם** |
| `DetailCard` | `App.jsx` + `atlas/card.jsx` | ציר הזמן, מסע הדורות |
| `Comments` | lazy מתוך `DetailCard.jsx` | ציר הזמן, מסע הדורות |
| `JourneyMap` | `App.jsx` + `atlas/map.jsx` | ציר הזמן, מסע הדורות |

`NotesBox` הוא היחיד שמגיע לשלושת המסכים — ולכן דווקא הוא זה שנשבר. השלושה האחרים
מגיעים למסע הדורות דרך `atlas/card.jsx`, **שמייבא את `styles.css` במפורש** כדי שהכרטיס
ייראה זהה. זו הסיבה שהם מעוצבים שם, וגם נקודת התורפה: המסך האחד מסתמך על כך שקובץ
הכניסה של מסך אחר ימשיך לייבא את גיליון הסגנונות שלו.

זה קרה בפועל: תיבת "הערה למנהל" רונדרה כחלונית לבנה פרושה על המפה, וגולש דיווח על כך.

**הדפוס הנכון: הסגנון בבעלות הרכיב.** ראה `src/components/NotesBox.css` שמיובא
מ-`NotesBox.jsx`. משתנים שקיימים רק ב-`styles.css` (`--navy-2`, `--bg-2`, `@keyframes cardIn`)
צריכים ערכי גיבוי שם.

**לפני שמוסיפים מחלקה לרכיב משותף — לבדוק לאילו מסכים הוא באמת מגיע (`grep` על הייבוא,
לא הנחה), ואז להעביר את הסגנון לרכיב.**

## RTL — שתי מלכודות אמיתיות

- **`border-radius` הוא מאפיין פיזי ולא לוגי.** `border-radius: 0 12px 12px 0` יעגל את
  הפינות בצד ההפוך ממה שהתכוונת. השתמש ב-`border-start-start-radius` וחבריו.
  (`.dc-verse` נושא את הבאג הזה עד היום — לא תוקן כי זה משנה כל כרטיס באתר.)
- **`inset-inline-start` הוא ימין ב-RTL.** לא שמאל.

## בנייה ופריסה

```
npm run dev        # שרת פיתוח, פורט 5173
npm run build      # vite build && node scripts/prerender.mjs
npm run smoke      # שער האיכות: טוען את dist בדפדפן ונופל על כל שגיאה
npm run verify     # build + smoke, מה שרץ ב-CI
npm run data       # מייצר מחדש את atlas-data.json ואת places.json
npm run preview    # הגשת dist, לבדיקת התוצר הסופי
```

`.claude/launch.json` מגדיר שתי תצורות: `simpleisrael` (פיתוח) ו-`simpleisrael-dist` (preview).

**אין בפרויקט מבחני יחידה ואין לינטר.** `npm run build` עובר גם על קוד שנופל בדפדפן —
זה קרה פעמיים (`treeOpen` לפני אתחול, הערת בלוק שבלעה שתי שורות import). **בנייה שעברה
אינה ראיה.**

הראיה היא `npm run smoke` (`scripts/smoke.mjs`). הוא מרים שרת סטטי שמגיש את `dist`
עם ה-rewrites וכותרות ה-CSP של `vercel.json`, ומעביר בדפדפן אמיתי עשר כתובות: ארבע
נקודות הכניסה, עמוד הפרטיות, אינדקס השערים, ותבנית אחת מכל סוג שה-prerender מייצר
(פריט, תקופה, מקום, אוסף). הוא נופל על חריגת JS, הפרת CSP, נכס חסר, סטטוס 4xx או מסך
שלא התרנדר. כל תעבורה החוצה נחסמת — גם לריצה זהה בכל פעם, וגם כדי שבדיקה לא תרשום
שורות אמיתיות ב-`si_trail`.

`.github/workflows/gate.yml` מריץ את זה בכל PR ובכל דחיפה ל-`main`, ובנוסף מוודא
ש-`public/atlas-data.json` ו-`src/data/places.json` מסונכרנים עם `maps.json`. **זה מה
שמאפשר למזג PR מהטלפון בלי לפתוח דפדפן.** הבדיקה הזאת תפסה בריצה הראשונה ש-תרצה נוספה
ל-`maps.json` בקומיט `7952e74` ו-`atlas-data.json` לא יוצר מחדש — כלומר מסע הדורות
הגיש תחנה חסרה בייצור.

הדפדפן ל-smoke אינו מותקן על ידי npm: `playwright-core` משתמש ב-Chrome (או Edge)
שכבר קיים במחשב, וכך גם בתמונת הריצה של GitHub.

### prerender

`scripts/prerender.mjs` (1174 שורות) מייצר **318 כתובות סטטיות**: 158 עמודי פריט,
13 עמודי תקופה, עמודי מקום ואוסף, `sitemap.xml`, `robots.txt`, `404.html` ו-`privacy.html`.
העמודים האלה הם מה שגוגל מפנה אליו, והם "שערים" — כרטיס עם שלוש דרכי כניסה לאתר החי.

`scripts/hero.mjs` שורף תמונות JPEG לכל פריט (`@resvg/resvg-js` + `jpeg-js`).
תוויות המקומות **נכתבות על המפה** שם — זה המקום היחיד שבו כן מציגים שמות על גבי המפה.

## נתונים

`src/data/*.json` — כ-472KB. `maps.json` הוא מקור האמת למסעות (276 תחנות).

**`places.json` הוא קובץ מיוצר** בידי `scripts/places-data.mjs` מתוך `maps.json` — אל
תערוך אותו ידנית.

`src/utils/placeNote.js` מסמן 35 תחנות שהסימון שלהן על המפה אינו המיקום האמיתי
(`מיקום מקורב` למקומות מחוץ למסגרת המפה, `זיהוי שנוי במחלוקת` לזיהויים שאינם מוסכמים).
ההערה מוצגת ברשימת התחנות ובכרטיס המסע **ולא על גבי המפה**.

## Supabase

| טבלה | תפקיד | גישה מהדפדפן |
|---|---|---|
| `comments` | תגובות פומביות | קריאה וכתיבה |
| `comments` עם `target_key='admin:notes'` | פניות פרטיות למנהל | כתיבה; קריאה רק דרך `admin_inbox` |
| `si_trail` | אירועי שימוש | **כתיבה בלבד**, אין policy לקריאה |
| `ask_log` | יומן ומטמון של סוכן השאלות | אין גישה כלל |

**דפוס חוזר: עמודה שכותבים אליה ולא קוראים ממנה.** `contact` ו-`notify_email` נשללו
מ-`select` עבור `anon` ברמת העמודה, כך שכתובת של מגיב אחד אינה נחשפת למגיב הבא.
הרשאה ברמת עמודה גוברת על כל policy.

סכמות SQL ב-`supabase/`. הן **תבניות** עם מקומות מסומנים למפתחות.

## 🔑 סודות — הכלל הכי חשוב בפרויקט

**מפתח Resend וטוקן טלגרם חיים אך ורק בתוך גופי פונקציות ב-Postgres. לעולם לא בריפו,
לעולם לא בצ'אט.**

- הקבצים ב-`supabase/` מכילים `<<< ... >>>` — המשתמש ממלא בעצמו
- העותק הממולא נשמר כ-`*.local.sql`, ש-`.gitignore` מכסה
- **GitHub push protection דלוקה וכבר חסמה דחיפה בפועל** אחרי `git add -A` שאסף מפתח
- מפתח Supabase ה-publishable (`sb_publishable_...`) ציבורי ובטוח בקוד; ה-service key לא
- לפני כל commit: `git ls-files -z | xargs -0 grep -lE "re_[A-Za-z0-9_]{15,}"`

לשחזור מפתח מהמסד: `select prosrc from pg_proc where proname = 'notify_new_comment';`

## מסך הניהול

`/admin` הוא תיבת הדואר: פניות פרטיות (`admin:notes`) ותגובות ציבוריות באותו מסך,
עם מחיקה, סימון "טופל" ותשובה. נבנה כדי לענות מהטלפון - עד אליו כל מענה למשתמש
דרש מחשב, ופניות פרטיות לא נקראו בשום ממשק.

הקריאה היא RPC אחד, `admin_inbox(p_token)`, שהוא `security definer` ומוודא את הטוקן
בתוך גוף הפונקציה - **אותו דפוס של `admin_delete_comment`, ובכוונה**: כך אין צורך
ב-`SUPABASE_SERVICE_KEY` ואין פונקציית שרת, והמסך עובד מכל דפדפן שבו הוקלד הטוקן.
הסכמה ב-`supabase/admin_inbox.sql` (תבנית - הטוקן ממולא ידנית).

שתי החלטות שכדאי להכיר:

- **תשובה לתגובה ציבורית** היא הוספה רגילה עם `parent_id`, ולכן הטריגר הקיים
  `notify_comment_reply` שולח את המייל למגיב בלי ששינינו בו דבר.
- **תשובה לפנייה פרטית היא `mailto:` ולא הודעה באתר.** פנייה פרטית אינה שרשור,
  והכותב השאיר כתובת כדי שיחזרו אליו. `mailto` פותח את אפליקציית המייל של הטלפון
  עם הציטוט בפנים, ולא דורש את מפתח Resend בשום מקום חדש.

## מדידה

`src/lib/trail.js`. **הקובץ נקרא `trail` והטבלה `si_trail` בכוונה, לא `analytics`
ו-`events`** — חוסמי פרסומות מסננים לפי תבנית בשם הצ'אנק ובנתיב הבקשה, ובגרסה הקודמת
הם חסמו את שניהם. אל תשנה את השמות האלה.

`startTrail()` נקרא בשלוש נקודות הכניסה. אירועים דרך `markOnce` (פעם אחת לטעינת עמוד).
קטלוג האירועים מתועד בראש הקובץ.

## סוכן השאלות — רדום

`api/ask.js` בנוי, נבדק, ופתוח **למנהל בלבד**. שכבת ההגבלה (מטמון, מכסה, תקציב) כתובה
ולא פעילה. **המצב המלא ושלושת צעדי ההערה ב-[api/README.md](api/README.md)** — קרא אותו
לפני שנוגעים.

## מוסכמות

- **הערות בקוד בעברית.** כל הקוד הקיים כתוב כך.
- מחרוזות ממשק הן ליטרלים בעברית בקוד, אין `strings.xml` ואין i18n. תרגום לאנגלית
  נשקל ונדחה במכוון: ה-UI הוא כשבוע עבודה, אבל 472KB התוכן הם פרויקט בפני עצמו,
  ואתר עם ממשק אנגלי ותוכן עברי גרוע מאתר עברי בלבד.
- שינוי סכמה ב-Supabase = קובץ SQL חדש ב-`supabase/`, לא עריכה של קיים.
- הודעות commit בעברית, מסבירות **למה** ולא רק מה.
